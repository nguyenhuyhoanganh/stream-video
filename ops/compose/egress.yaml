api_key: devkey
api_secret: meetly_dev_secret_0123456789abcdef
# egress chạy network_mode: host — nhìn LiveKit/Redis qua cổng publish trên host
# (livekit --node-ip 127.0.0.1 quảng bá ICE 127.0.0.1, container thường không với tới)
ws_url: ws://localhost:7880
redis:
  address: localhost:6379
insecure: true
logging:
  level: info
